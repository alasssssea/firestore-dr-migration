# Firestore 容灾迁移工具（fsdr）· 使用说明

本工具把数据从**单区域（regional）Firestore（MongoDB 兼容）** 迁移到**多区域（multi-region）Firestore（MongoDB 兼容）**，用于容灾/位置变更场景。因为 Firestore 的位置（location）一旦建库就不可更改，所以「换成多区域」本质上是：**新建一个多区域库 → 把数据拷过去 → 追平增量 → 割接**。

工具只做 **Firestore → Firestore**，不再包含任何 MongoDB 自建源、oplog、旧版驱动相关能力（那些是上游 `mongodb-to-new` 的东西，本 fork 已全部剥离）。

核心能力：

- 🌐 **浏览器控制台（console 模式）** —— 网页里配置、评估、启动、监控迁移，中英文切换。
- 📦 **全量拷贝（migrate）** —— 一次性把源库所有集合的数据 + 索引拷到目标库。
- 🔴 **全量 + 增量追尾（live）** —— 先记录变更流位点，再做全量拷贝，然后自动切换到 CDC（变更流）持续追平，直到割接。
- ✅ **迁移后校验（verify）** —— 比对源/目标文档数量，可选按内容哈希比对。
- 📊 **控制平面** —— `/metrics`、健康检查、状态 API、实时进度/延迟看板、割接就绪信号（cutover readiness）。

> ⚠️ **计费提醒**：源库、目标库、变更流都会产生 Firestore 费用。演示/测试完请及时 teardown。

---

## 目录

1. [环境要求与前置条件](#1-环境要求与前置条件)
2. [快速开始](#2-快速开始)
3. [连接串格式](#3-连接串格式)
4. [运行方式一：浏览器控制台（推荐）](#4-运行方式一浏览器控制台推荐)
5. [运行方式二：命令行 CLI](#5-运行方式二命令行-cli)
6. [标准割接流程](#6-标准割接流程)
7. [变更流（change stream）启用](#7-变更流change-stream启用)
8. [运行时产生的文件](#8-运行时产生的文件)
9. [常见问题排查](#9-常见问题排查)

---

## 1. 环境要求与前置条件

- **Go 1.21+**（本仓库在 Go 1.25 上开发/验证）。
- **源**：一个单区域 Firestore（MongoDB 兼容）数据库。
- **目标**：一个**你已经手动建好的、空的多区域 Firestore（MongoDB 兼容）数据库**。
  - 本工具**不创建数据库**。请先在 Cloud Console / gcloud 建好目标多区域库，再把连接串填进来。
- **做 live（增量）时**：必须先在**源库**上**手动创建 change stream**（见 [第 7 节](#7-变更流change-stream启用)）。工具只做预检，不会替你启用基础设施。
- Linux / macOS（脚本为 bash）。

检查 Go：

```bash
go version
```

---

## 2. 快速开始

```bash
# 1) 进入目录
cd firestore-DR-migration

# 2) 编译（产物为当前目录下的 ./fsdr）
go build -o fsdr ./cmd/fsdr

# 3a) 方式一：启动浏览器控制台（推荐，零配置，表单里直接配）
./fsdr -mode console -metrics-addr :9090
#   浏览器打开 http://localhost:9090/

# 3b) 方式二：直接命令行跑一次全量拷贝
./fsdr -config=my_config.json -mode=migrate

# 3c) 全量 + 增量追尾（需源库已启用 change stream）
./fsdr -config=my_config.json -mode=live
```

---

## 3. 连接串格式

源、目标都是 Firestore，两端连接串都必须带 Firestore 必需参数，尤其是 **`retryWrites=false`**（Firestore 不支持可重试写）：

**SCRAM 用户名/密码：**

```
mongodb://<user>:<pass>@<uid>.<location>.firestore.goog:443/<databaseId>?loadBalanced=true&tls=true&retryWrites=false&authMechanism=SCRAM-SHA-256
```

**服务账号 OIDC（长跑推荐，免密码、令牌自动续期）：**

```
mongodb://<uid>.<location>.firestore.goog:443/<databaseId>?loadBalanced=true&tls=true&retryWrites=false&authMechanism=MONGODB-OIDC&authMechanismProperties=ENVIRONMENT:gcp,TOKEN_RESOURCE:FIRESTORE
```

- `<location>`：源为单区域（如 `asia-northeast1`），目标为多区域（如 `nam5`、`eur3` 等）。
- 控制台里源连接串是**原样透传**——直接粘贴上面完整的 URI 即可；目标连接串由控制台按你填的 endpoint / 库 ID / 认证方式自动拼好（已包含 `retryWrites=false`）。

---

## 4. 运行方式一：浏览器控制台（推荐）

```bash
./fsdr -mode console -metrics-addr :9090
```

浏览器打开 `http://localhost:9090/`，按步骤走：

1. **源 Firestore（单区域）** —— 粘贴源连接串，点「检测源服务器」，会列出库/集合。
2. **选择要迁移的库/集合**。
3. **源库 → Firestore 目标映射** —— 为每个源库指定一个**已建好的空多区域目标库**（可搜索已有库）。
4. **预迁移评估（可选）** —— 抽样扫描，给出并发建议。
5. **模式、并发并开始** —— 选 `migrate`（全量一次性）或 `live`（全量 + 增量追尾），点开始，本页实时看进度与延迟。

看门狗脚本（可选，保活控制台）：

```bash
./run-console.sh :9090          # 前台守护，console 挂了自动拉起
./restart-console.sh :9090      # 安全重启（有在跑的任务会拒绝，除非 --force）
```

---

## 5. 运行方式二：命令行 CLI

| 模式 | 说明 |
|------|------|
| `migrate` | 一次性全量拷贝（数据 + 索引），完成即退出。 |
| `live` | 先记录变更流位点 → 全量拷贝 → 自动转 CDC 持续追平，长驻运行。 |
| `live-only` | 只做增量，跳过全量；从上次保存的 resume token（或 `-live-start-timestamp` 指定位点）开始。 |
| `retry-dlq` | 重放死信队列（DLQ）里之前写失败的文档。 |
| `assess` | 只读的迁移前检查（连通性、变更流、索引清单），打印报告。 |
| `verify` | 迁移后校验：比对源/目标文档数（加 `-verify-hash` 再比内容哈希）。 |
| `wizard` | 交互式生成配置文件后退出。 |
| `console` | 启动浏览器控制台。 |

```bash
./fsdr -help          # 查看全部参数
```

---

## 6. 标准割接流程

推荐用 `live` 模式做**一次性迁移 + 增量追平 + 割接**（非长期双活）：

1. **启用源库 change stream**（见第 7 节）。
2. **启动 live**：工具先记录变更流位点（保证零丢失），再做全量拷贝，然后自动转增量追平。
   > 全量拷贝必须在**变更流保留期内**完成（保留期最长 7 天），否则增量追平会有断档。
3. **观察延迟**：控制台/日志里看增量延迟（lag）。当延迟趋近 0，工具给出**割接就绪（ready）** 信号。
4. **应用层停写**：由**你**在应用层冻结对源库的写入（工具**绝不**替你冻结源写入）。
5. **排空剩余事件**：工具把最后一批变更消费完。
6. **校验**：`./fsdr -mode=verify`（可加 `-verify-hash`）比对源/目标。
7. **割接**：确认无误后，由你把应用切到目标多区域库，并停止工具。

> 说明：源已是 Firestore，其数据本就是 Firestore 合法数据，因此本 fork **不做 `_id` 类型转换**（`_id` 原样透传），保证目标与源逐条一致，校验才能对得上。

---

## 7. 变更流（change stream）启用

Firestore 的变更流是 **Preview** 特性，**必须手动、按库创建**——**没有 gcloud 命令、也不会随建库自动开启**。做 `live` / `live-only` 前，请在 Cloud Console 里为**源库**创建：

1. 打开 **Databases** 页，选中源 Firestore（MongoDB 兼容）库，进入 **Firestore Studio**。
2. 在 **Explorer** 面板找到 **Change streams** 节点 → **More actions** → **Create change stream**。
3. 填名称、scope（要迁移的库/集合）、保留期（最长 7 天），保存。
4. 需要 **Datastore Index Admin**（`roles/datastore.indexAdmin`）角色。

文档：<https://docs.cloud.google.com/firestore/mongodb-compatibility/docs/change-streams>

如果没启用就跑 live，工具打开变更流会失败，并打印上述指引（预检）。

---

## 8. 运行时产生的文件

- `resumeToken*.json` —— 变更流位点检查点（断点续跑用）。
- `initialMigrationState*.json` —— 全量阶段状态。
- `*.dlq` / DLQ 相关文件 —— 写失败的文档，供 `retry-dlq` 重放。
- `*.log` —— 运行日志。

这些是运行时状态，已在 `.gitignore` 忽略，不要提交。

---

## 9. 常见问题排查

- **`failed to create change stream ...`**：源库没建 change stream，或权限不足。见第 7 节。
- **连不上 / 认证失败**：检查连接串是否带全 `loadBalanced=true&tls=true&retryWrites=false` 及正确的 `authMechanism`；OIDC 模式确认运行环境有可用的 ADC / 服务账号。
- **全量拷贝太慢、超出保留期**：提高并发，或缩短数据量后再迁；确保拷贝在变更流保留期内完成。
- **校验数量对不上**：确认已在应用层停写并排空增量后再 verify；必要时 `-verify-hash` 逐条比对。
